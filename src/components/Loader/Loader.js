import React from 'react';
import './Loader.css'; // We will define the loader's CSS here

const Loader = () => {
  return (
    <div className="loader">
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;
